---
title: 'Skill: resolver/sync'
description: >-
  Implementation of `/resolver sync` verb — scans filesystem for skill/command/agent recipients
  without routes, generates stubs from frontmatter, writes routes/*.yaml. Default --dry-run per D-2
  founder decision. --apply writes; --auto-pr writes + branch + commit + push + opens PR (Tier C).
source_path: 06-ai-ops/skills/resolver/sync/SKILL.md
source_hash: 135b3ac4d967270cc2dd18d49ebd87265e8c44e11dc6dd6d55ea59ea5cae0b11
generated_at: '2026-05-23T08:45:47.929Z'
generated_by: docs-engine v1.2.0
category: skill
language: vi
translated: true
translated_at: '2026-05-23T09:00:00.000Z'
translated_by: docs-translate-inline v1.0 (claude-code session)
translation_model: claude-opus-4-7
translated_source_hash: 135b3ac4d967270cc2dd18d49ebd87265e8c44e11dc6dd6d55ea59ea5cae0b11
---

{/* generated-by: docs-engine v1.2.0 */}

## Khi nào dùng

Được invoke bởi `resolver/orchestrator` khi subcommand là `sync`.

## Process

1. Parse flags: --dry-run (mặc định) | --apply | --auto-pr | --kind=\<k>
2. Invoke `scripts/resolver/sync.cjs` với args đã parse
3. Render output theo mode:
   - --dry-run: in diff đề xuất (không ghi gì)
   - --apply: confirm + ghi atomically; báo cáo số stub mới
   - --auto-pr: + git ops + gh pr create; in PR URL

## D-2 founder decision

Mode mặc định là `--dry-run` (warn-only). Founder enable `--apply` hoặc
`--auto-pr` một cách explicit để commit thay đổi.

## Architect T-7 constraints

- Auto-PR chỉ ROUTE stubs, KHÔNG BAO GIỜ RECIPIENT stubs
- Rate-limited 1 PR mỗi 6h mỗi branch (lock file:
  `.archives/resolver-sync.lock`)
- Working tree phải clean (không có thay đổi không liên quan) cho --auto-pr

## Error handling

- `WorkingTreeDirty` → từ chối với gợi ý stash
- `SyncLocked` → hiển thị lock age; gợi ý wait hoặc manual unlock
- `GHCLIMissing` → install hint
- `GHAuthExpired` → gợi ý `gh auth login`

## Xem thêm

- Engine: `scripts/resolver/sync.cjs`
- Architect T-7 + D-2: `.archives/brainstorming/resolver/17-outside-voice.md`
- Spec: `wiki/capabilities/resolver/spec.md` §11.6
