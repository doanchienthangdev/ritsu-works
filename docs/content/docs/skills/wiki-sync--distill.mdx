---
title: 'Skill: wiki-sync/distill'
description: |
  v3.0 CORE ENGINE — LLM-driven entity extraction from source chunks into
  separately-projected wiki entity pages (concept / observation / decision / idea)
  with full citation back to source via ops.knowledge_extractions. Per-type
  model picker (Haiku for concept+idea — cheap; Sonnet for observation+decision
  — fidelity). Reframes wiki-sync from verbatim-projection (v1+v2) to
  distill+extract (v3.0). Invoked by ingest SKILL Step 6 after chunks come out
  of the adapter; writes derived entity pages + knowledge_extractions rows.
source_path: 06-ai-ops/skills/wiki-sync/distill/SKILL.md
source_hash: b1764a8b7d3ebd0c40be51ed7ecab3209ead5a8587678474e7682dd43e4b71fe
generated_at: '2026-05-19T05:20:46.376Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_source_hash: b1764a8b7d3ebd0c40be51ed7ecab3209ead5a8587678474e7682dd43e4b71fe
translated_at: "2026-05-19T15:00:00Z"
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# wiki-sync / distill (v3.0 core engine)

## Khi nào skill này chạy

- Được gọi bởi `wiki-sync/ingest` Bước 6 sau khi Bước 5 (extract chunk từ source adapter) hoàn tất.
- MỘT lần gọi mỗi source ingest. Loop nội bộ trên các chunk.
- CHỈ chạy khi `feature-flags.yaml::wiki_sync_distill_enabled` là `true` (mặc định v3.0). Nếu `false` HOẶC nếu cờ `--verbatim` được set, pipeline ingest fall through sang path viết single-page v2.0 (không distill, không entity page, không extraction).

## Đầu vào

- `source_page_id` (uuid) — trang source RECORD đã được INSERT vào `ops.knowledge_pages` ở Bước 5 (với `extracted_from_source_id = NULL` vì source CHÍNH LÀ source).
- `chunks` (mảng `{ index: int, text: string, token_count: int }`) — đầu ra của bước chunking của adapter.
- `source_kind` (text) — để tune prompt theo adapter (ví dụ: chunk growth playbook cần system prompt khác chunk research paper).
- `license_status` (enum) — đọc từ frontmatter source RECORD; cung cấp thông tin cho attribution-watcher (skill Sprint 4).

## Đầu ra

- INSERT N dòng vào `ops.knowledge_pages` cho các derived entity (`page_type` IN `concept` | `observation` | `decision` | `idea`; `extracted_from_source_id = source_page_id`; `legacy_v2_verbatim = false`)
- INSERT M dòng vào `ops.knowledge_extractions` (trục citation: `source_page_id`, `source_chunk_index`, `derived_page_id`, `link_type`, `confidence`, `llm_model`, `extraction_cost_usd`, `raw_quote`)
- INSERT N dòng vào `ops.knowledge_links` (graph quan hệ: từ trang source RECORD → các trang derived entity; `link_type` IN `extracted_concept` | `extracted_observation` | `extracted_decision` | `extracted_idea`)
- WRITE N file Markdown tại `wiki/<source-slug>/<page_type>s/<slug>.md` với frontmatter v3.0 (xem spec Frontmatter bên dưới); `<page_type>s` là tên folder số nhiều (concepts/observations/decisions/ideas)
- Một dòng trong `ops.agent_runs` tổng kết distill (cost, đếm entity, phân bố confidence)
- Cost-bucket attribution: `ai-ops-knowledge`; task_kind theo source_kind (xem mục Chi phí)

## Picker model per-type (CTO NIT 5)

| Loại entity | Model | Chi phí per-chunk | Lý do |
|---|---|---|---|
| `concept` | `claude-haiku-4-5` | $0.005-0.02 | Concept là cụm danh từ; recall của Haiku là ổn |
| `idea` | `claude-haiku-4-5` | $0.005-0.02 | Suy nghĩ chưa hoàn chỉnh; bar fidelity thấp hơn |
| `observation` | `claude-sonnet-4-6` | $0.025-0.10 | Tuyên bố thực nghiệm cần fidelity với phrasing source |
| `decision` | `claude-sonnet-4-6` | $0.025-0.10 | Lựa chọn framework cần sắc thái |

Mỗi chunk được gửi đến CẢ một call Haiku (cho concept + idea extraction) VÀ một call Sonnet (cho observation + decision extraction). Hai đầu ra merge vào tập extraction của chunk.

Tối ưu: khi `chunks.length > 20` AND budget chặt, batch các chunk trong call Haiku (một prompt, nhiều chunk); Sonnet giữ per-chunk (fidelity quan trọng hơn).

## Ngữ nghĩa confidence (CTO NIT 4) — QUAN TRỌNG

`confidence` được LLM trả về là tín hiệu **COARSE 3-BUCKET** trong thực tế:

- ~0.6 = "this is plausibly an entity but I'm not sure"
- ~0.8 = "this is clearly an entity but I had to interpret"
- ~0.95 = "this is unambiguously an entity, source phrasing is direct"

**KHÔNG được coi giá trị số là xác suất đã calibrate.** Các hook/query tương lai tính "expected error rate at threshold X" so với `confidence` đang dùng sai tín hiệu. Framing 3-bucket khớp với hành vi self-report của cả Haiku và Sonnet về mặt empirical.

Migration 00031 cho phép khoảng đầy đủ 0..1 trong cột (`CHECK (confidence >= 0 AND confidence <= 1)`), nhưng các consumer downstream nên round-treat giá trị thành 3 bucket.

Disposition ngưỡng (theo spec §0 A6):
- `>= 0.85` → auto-accept; INSERT trực tiếp; trang `review_state = 'auto_accepted'`
- `0.6 <= conf < 0.85` → queue; INSERT nhưng trang `review_state = 'pending_review'`; xuất hiện trong queue `/wiki review`
- `< 0.6` → auto-reject; KHÔNG INSERT; được log vào `ops.agent_runs.output_payload.rejected_extractions` cho minh bạch chi phí

## Quy trình

### Bước 1 — Kiểm tra pre-flight

- Verify `wiki_sync_distill_enabled = true` trong feature-flags.yaml. Nếu false, abort với `state_payload.distill_skipped = true` và pass lại ingest cho verbatim fallthrough.
- Verify source_page_id tồn tại trong `ops.knowledge_pages` và có `extracted_from_source_id IS NULL` (nó CHÍNH LÀ source).
- Verify `license_status` được set trên frontmatter source. Nếu NULL, prompt founder qua AskUserQuestion: which license category? (attribution-watcher Sprint 4 cần điều này.)
- Ước tính chi phí: `chunks.length × per-type-avg`. Nếu vượt cap per-task-kind (xem mục Chi phí), leo thang qua Tier B `AskUserQuestion`.

### Bước 2 — Extraction per-chunk (loop)

Cho mỗi chunk trong `chunks`:

```
HAIKU call (concept + idea):
  POST https://api.anthropic.com/v1/messages
  model: claude-haiku-4-5
  max_tokens: 800
  system: |
    Extract knowledge ENTITIES from this source chunk. Output JSON only.
    
    Entity types you may extract:
      - "concept": a recurring noun phrase / vocabulary term the source defines or
        relies on (e.g., "product-led growth", "wedge", "ICP"). Slug in
        kebab-case derived from the canonical noun.
      - "idea": an unfinished thought the source raises but doesn't resolve
        (e.g., "what if X interacted with Y?"). Slug derived from the question
        topic.
    
    For EACH entity, output:
      {
        "type": "concept" | "idea",
        "name": "<canonical noun phrase>",
        "slug": "<kebab-case>",
        "summary": "<1-2 sentence summary of how the source defines/raises it>",
        "confidence": 0.6 | 0.8 | 0.95,
        "raw_quote": "<≤300 chars literal text from source supporting this>"
      }
    
    Rules:
      - confidence is a 3-bucket signal: 0.6 = plausibly an entity but unsure,
        0.8 = clearly an entity but I had to interpret, 0.95 = unambiguous.
      - Do NOT extract proper nouns of people/companies (those are other page_types).
      - Do NOT invent slugs; derive from canonical noun phrase.
      - If the chunk has no extractable entities, return [].
      - Output JSON array only. No prose.
  user: <chunk.text>

SONNET call (observation + decision):
  POST https://api.anthropic.com/v1/messages
  model: claude-sonnet-4-6
  max_tokens: 1500
  system: |
    Extract knowledge ENTITIES from this source chunk. Output JSON only.
    
    Entity types you may extract:
      - "observation": an empirical claim with source attribution
        (e.g., "Companies that did X had Y outcome — per Source A, Year B").
        Slug: kebab-case derived from the claim's main proposition.
        Observations MUST cite the source claim (often inline in chunk).
      - "decision": a framework choice or strategic ruling the source recommends
        (e.g., "Pick wedge before brand"). Slug: imperative kebab-case
        (e.g., "always-position-before-tactics").
    
    For EACH entity, output:
      {
        "type": "observation" | "decision",
        "name": "<canonical claim or ruling>",
        "slug": "<kebab-case>",
        "summary": "<2-3 sentence summary; for observations include attribution>",
        "confidence": 0.6 | 0.8 | 0.95,
        "raw_quote": "<≤500 chars literal text from source supporting this>"
      }
    
    Rules:
      - confidence is a 3-bucket signal (see Haiku prompt).
      - Observations MUST have a citable source claim — if the chunk just makes
        an assertion without attribution, prefer "decision" or skip.
      - Decisions are imperative ("do X" or "pick X over Y") with rationale.
      - Output JSON array only. No prose.
  user: <chunk.text>
```

Parse mỗi response LLM dưới dạng JSON. Validate shape. Reject các entity có `confidence < 0.6` (auto-reject; được log vào `output_payload.rejected_extractions`).

### Bước 3 — Normalization slug + kiểm tra collision

Cho mỗi entity được extract:
- Normalize slug: lowercase, thay whitespace bằng `-`, strip non-alphanumeric ngoại trừ `-`, collapse các `-` lặp.
- Kiểm tra `ops.knowledge_pages` cho dòng hiện có với cùng `(page_type, slug)`. Nếu tồn tại:
  - Nếu `extracted_from_source_id IS NOT NULL` của dòng hiện có → đây là ứng viên CROSS-SOURCE DEDUP. Đừng INSERT trang mới; thay vào đó INSERT một dòng `knowledge_extractions` trỏ tới trang HIỆN CÓ (source này giờ cite entity hiện có). Pass dedup ở Bước 4 cũng có thể flag điều này cho `pending_review` nếu signal mâu thuẫn.
  - Nếu `extracted_from_source_id IS NULL` của dòng hiện có (được tạo thủ công HOẶC v2.0 legacy) → vẫn cite qua knowledge_extractions nhưng KHÔNG đánh dấu là conflict.
- Nếu không collision → INSERT dòng `knowledge_pages` mới.

### Bước 4 — Cross-chunk dedup (chỉ within-source; Sprint 3 ship cross-source semantic dedup)

Trong source ingest này, nếu hai chunk extract cùng `(page_type, slug)`, merge thành MỘT trang với HAI dòng `knowledge_extractions` (mỗi dòng cho một source_chunk_index).

### Bước 5 — Viết các trang derived entity

Cho mỗi derived entity duy nhất:

```markdown
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---
type: <concept | observation | decision | idea>
title: <name>
slug: <slug>
source_ref: <source_page_id slug or wiki path>
extracted_from_source: wiki/<source-slug>/source.md
license_status_inherited: <from source frontmatter>
confidence: <max confidence across this entity's extractions>
review_state: <auto_accepted | pending_review>
generated_by: wiki-sync/distill v0.1
created_at: <ISO timestamp>
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

# <name>

<summary from the highest-confidence extraction>

## Sources cited

<for each knowledge_extractions row:>
- **From [<source-title>](wiki/<source-slug>/source.md#chunk-<source_chunk_index>)** (confidence: <conf>):

  > "<raw_quote>"

<for cross-source dedup — when Sprint 3 dedup merges this entity across multiple sources, multiple "From X" entries appear>

<!-- generated-by: wiki-sync/distill v0.1 -->
```

Viết vào `wiki/<source-slug>/<page_type>s/<slug>.md` (layout v4.0 source-grouped; `<page_type>s` là folder số nhiều — concepts/observations/decisions/ideas). File path đi vào `knowledge_pages.file_path`.

> **v4.0 composite UNIQUE:** slug uniqueness cho derived entity giờ là `(extracted_from_source_id, slug)` (không phải global). Hai source có thể hợp pháp extract cùng slug (ví dụ: `wedge` từ cả `pg-do-things/` và `blank-4-steps/`) như các entity riêng biệt — kiểm tra collision của Bước 3 được scope per-source, không phải global.

### Bước 6 — Viết các dòng citation knowledge_extractions

INSERT một dòng mỗi edge (source_chunk → derived_entity):

```sql
INSERT INTO ops.knowledge_extractions
  (source_page_id, source_chunk_index, derived_page_id, link_type,
   confidence, llm_model, extraction_cost_usd, raw_quote)
VALUES
  ($source_page_id, $chunk_index, $derived_page_id,
   'extracted_' || $entity_type,    -- one of 4 strict enum values
   $confidence, $llm_model, $per_extraction_cost, $raw_quote);
```

### Bước 7 — Viết các edge graph knowledge_links

INSERT một dòng mỗi (source RECORD → derived entity) cho traversal graph:

```sql
INSERT INTO ops.knowledge_links
  (source_page_id, target_page_id, link_type)
VALUES
  ($source_page_id, $derived_page_id, 'extracted_' || $entity_type);
```

(`knowledge_links.link_type` là free-text theo migration 00006 — giá trị `extracted_*` là hợp lệ; các link regex-extracted v2.0 như `defines`/`see_also` vẫn hoạt động.)

### Bước 8 — Embed các trang derived entity (defer hoặc inline)

Nếu `OPENAI_API_KEY` có sẵn AND `wiki_sync_embeddings_backfill = true`:
- Tạo embedding inline (1 call mỗi trang entity = 1 lần gọi OpenAI text-embedding-3-small).
- INSERT các dòng `ops.knowledge_embeddings`.

Nếu `OPENAI_API_KEY` thiếu (trạng thái deferred hiện tại của v2.0 theo retrospective):
- Set `knowledge_pages.metadata.embeddings_deferred = true`.
- Cron hằng giờ `wiki-embeddings-backfill` pick up (theo cập nhật Sprint 3 cho SKILL embeddings-backfill).

### Bước 9 — Ghi nhận chi phí

INSERT một dòng `ops.cost_attributions` mỗi LLM call (Haiku + Sonnet × các chunk). Roll up thành một summary `ops.agent_runs`:

```
{
  agent_slug: "wiki-sync/distill",
  task_kind: "wiki-distill-pdf" | "wiki-distill-folder" | "wiki-distill-other",
  cost_usd: <sum of Haiku + Sonnet>,
  input_payload: { source_page_id, chunks_count },
  output_payload: {
    entities_extracted: { concept: N, observation: M, decision: K, idea: J },
    auto_accepted: <count>,
    pending_review: <count>,
    rejected: <count>,
    by_chunk: [{ chunk_index, entities, llm_models_used, cost }]
  }
}
```

### Bước 10 — Phát event

INSERT dòng `ops.events`:
```
event_type: 'ritsu.wiki.distill_completed'
payload: { source_page_id, entities_count, pending_review_count, total_cost_usd }
```

Nếu `pending_review_count > 0`: heads-up Telegram cho founder được batch vào cron `wiki-review-queue-digest` hằng ngày (Sprint 4 ship).

## Chi phí (cap per-task-kind theo spec §0)

| task_kind | Cap per-source | Notes |
|---|---|---|
| `wiki-distill-pdf` | $2.00 | 5-20 chunks × ~$0.05-0.10 trung bình (Haiku + Sonnet) |
| `wiki-distill-folder` | $15.00 (Tier B trên $5) | Per-paper × N paper; được nâng theo Muse M6 |
| `wiki-distill-other` | $0.50 | URL / Markdown / YouTube / meeting |

Hook `pre-llm-call-budget` enforce. Nếu ước tính chi phí vượt cap → Tier B founder confirm qua AskUserQuestion.

## Các chế độ lỗi

| Triệu chứng | Phản ứng |
|---|---|
| Output JSON của Haiku malformed | Retry một lần với prefix "OUTPUT VALID JSON" rõ ràng. Nếu lần thứ hai fail, skip extraction concept/idea của chunk; tiếp tục Sonnet. Log vào `output_payload.malformed_responses`. |
| Output JSON của Sonnet malformed | Cùng pattern retry. |
| Slug collision với trang verbatim legacy v2.0 | Cite qua knowledge_extractions; KHÔNG modify trang legacy. `review_state` của derived entity mới giữ auto_accepted/pending_review dựa trên confidence extraction. |
| Tất cả entity dưới 0.6 confidence | Trang source RECORD vẫn được viết nhưng không có entity page. Founder nhận Telegram heads-up "Source X distilled with 0 entities — review source quality or distill prompts". |
| Cap chi phí per-task-kind vượt giữa ingest | Pause; phát Tier B AskUserQuestion với chi phí hiện tại + chi phí còn lại dự kiến. Founder approve continue HOẶC cancel. |
| `license_status` thiếu trên source RECORD | AskUserQuestion cho founder ("Which license category for this source?"); cache câu trả lời vào frontmatter source; tiếp tục. |
| OPENAI_API_KEY thiếu trong Bước 8 | Soft-defer theo pattern G3 hiện tại của v2.0. Embedding được backfill bởi cron hằng giờ. Không alert founder (xử lý bình thường). |

## Kỷ luật HITL

- Mặc định Tier A (ghi additive; reversible qua `/wiki merge --undo` nếu entity nào sai).
- Tier B nếu vượt cost-cap HOẶC queue pending_review tăng (founder daily digest).
- Tier C không bao giờ — distill không bao giờ ghi không thể đảo ngược (chỉ soft-delete qua merge).

## Liên quan

- Pipeline: `06-ai-ops/sops/SOP-INGEST-001-wiki-sync/{README,flow.yaml}` Bước 6
- Trục citation: migration `supabase/migrations/00031_wiki_distillation.sql` Block A
- Dedup: `06-ai-ops/skills/wiki-sync/dedup/SKILL.md` (Sprint 3)
- Review: `06-ai-ops/skills/wiki-sync/review/SKILL.md` (Sprint 4)
- Retrieval: `06-ai-ops/skills/wiki-sync/ask/SKILL.md` (Sprint 4 — retrieval entity-first)
- Audit: `06-ai-ops/skills/wiki-sync/audit/SKILL.md` (Sprint 4 — check citation integrity)
- Spec: `wiki/capabilities/wiki-sync-from-refs/spec.md` v3.0 §3
- Decision: `ops.decisions[562b2e4e-7f50-47f2-a3bb-1291c30f6709]`

## Phiên bản

- **v0.1** (Sprint 2 của wiki-sync-from-refs v3.0.0, 2026-05-18): distill engine đầu tiên. Picker model per-type (Haiku cho concept+idea; Sonnet cho observation+decision). Confidence dưới dạng tín hiệu 3-bucket được document. Slug collision within-source dedup; cross-source semantic dedup hoãn sang Sprint 3 (`dedup` SKILL).
