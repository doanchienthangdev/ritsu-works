---
title: 'Skill: wiki-sync/link-extractor'
description: |
  Sprint 1 baseline: trích xuất chỉ bằng regex theo link-inference-rules của Bài #14.
  Sprint 2 PR3 (v2.0) đã thêm rule `related_concept` cho các tham chiếu chéo informal
  concept→concept có tiền tố "(see also)" / "(cf.)" (G2 disposition).
  Sprint 4 sẽ thêm LLM-fallback (escalate sang LLM khi regex tìm thấy < 3 link
  trong một chunk > 1000 token) được gate bằng feature flag `wiki_sync_llm_fallback`.
source_path: 06-ai-ops/skills/wiki-sync/link-extractor/SKILL.md
source_hash: 8e313544e05eab2c7d2186a94e96601ac510e5759a140347616448b7ef7b48fe
generated_at: '2026-05-19T05:20:46.380Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_source_hash: 8e313544e05eab2c7d2186a94e96601ac510e5759a140347616448b7ef7b48fe
translated_at: 2026-05-19T15:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# wiki-sync / link-extractor (Sprint 1 baseline + Sprint 2 PR3 G2 + spec LLM-fallback của Sprint 3 PR5)

## Định dạng link cross-page v4.0 (layout source-grouped)

Dưới layout source-grouped v4.0, các wiki link cross-package mang đầy đủ ngữ cảnh đường dẫn: `wiki/<source-slug>/concepts/<entity>.md#anchor` resolve thực thể trong source package của nó (UNIQUE composite `(extracted_from_source_id, slug)` nghĩa là cùng một slug có thể tồn tại hợp lệ trên nhiều package, nên tiền tố source-slug giúp khử mơ hồ). Các pattern regex hiện có trong `knowledge/link-inference-rules.yaml` là layout-agnostic (chúng khớp trên nội dung slug, không phải trên đường dẫn `wiki/concept/` cứng), nên các pattern không cần cập nhật — nhưng các row link được chèn nên mang đường dẫn source-grouped trong `source_text` / `extracted_from_section` để resolve về sau bởi `wiki-sync/audit` và `/wiki ask`. Các link-list tra cứu ngược `wiki/_index/` (được tái tạo bởi `wiki-sync/index-rebuild`) cung cấp một điểm neo duy nhất cho "tất cả các package có concept tên X".

## Đường LLM-fallback v2.0 (thêm vào Sprint 3 PR5)

Đằng sau feature flag `wiki_sync_llm_fallback` trong `knowledge/feature-flags.yaml`
(mặc định `false` cho v2.0; Sprint 4 có thể bật thành true sau khi đo baseline).

### Khi nào LLM-fallback kích hoạt (v0.2+)

Sau khi pass regex hoàn thành, đếm số link đã trích xuất bằng regex:
- Nếu `links_extracted >= 3` mỗi 1000 token chunk_text → DỪNG. Coverage regex là đủ.
- Nếu `links_extracted < 3` mỗi 1000 token VÀ chunk_text chứa các pattern giống thực thể (danh từ riêng, [[brackets]] không có slash, "according to ...", "per \<name>") → LLM-fallback kích hoạt.

### Gọi API LLM-fallback

```
POST https://api.anthropic.com/v1/messages
  model: claude-haiku-4-5 (cheap, structured-output-good)
  system: |
    Extract knowledge-graph links from this chunk. Output JSON array of:
    [{ link_type, target_slug, target_type, source_text, confidence }]
    
    Allowed link_types: (list from link-inference-rules.yaml)
    Allowed target_types: (page_type enum values)
    
    Rules:
    - Only extract links to entities likely to have wiki pages
    - confidence in [0.0, 1.0]; reject < 0.6
    - Do not invent slugs; use kebab-case derivations from the source_text
    - Return empty array if no clear links
  user: <chunk_text>
```

Chi phí: ~$0.005-$0.02 mỗi chunk rơi xuống fallback (haiku rẻ).

### Validation + dedup

Với mỗi link do LLM đề xuất:
- Kiểm tra slug target có tồn tại trong `ops.knowledge_pages` không (resolved → `target_page_id`)
- Nếu không, để dưới dạng orphan link (target_page_id NULL) — giống hành vi regex
- Bỏ qua các bản trùng của link regex đã trích xuất (cùng source_text)
- Áp dụng ngưỡng confidence (drop \< 0.6)

### Audit + cost attribution

Mỗi lần gọi LLM-fallback đều ghi vào:
- `ops.cost_attributions` với `model='claude-haiku-4-5'`, `task_kind='wiki-link-llm-fallback'`
- Chi phí được attribute về row ingestion_jobs cha (không phải row của chính nó)

Cap per-task-kind (được thêm vào README SOP-INGEST-001 trong PR này): `wiki-link-llm-fallback` = $0.10/trang (thường 5-20 chunk × $0.01 mỗi cái).

## Hành vi STUB v0.1 (commit NÀY)

Khi `wiki_sync_llm_fallback` là `false`:
- Bỏ qua LLM-fallback hoàn toàn.
- Trả về kết quả chỉ regex.
- Log vào `ops.agent_runs.output_payload`: `llm_fallback_skipped: { reason: 'feature_flag_off', would_have_triggered: <bool> }` để Sprint 4 phân tích tỷ lệ hit.

## Sản phẩm bàn giao Sprint 3 PR5 (commit này)

- Spec cho đường LLM-fallback v0.2 được tài liệu hóa (ở trên)
- Không thay đổi code — chỉ cập nhật prose SKILL + kiểm tra feature-flag
- Cờ `wiki_sync_llm_fallback` đã được khai báo trong `knowledge/feature-flags.yaml` của PR3

## Sprint 4 PR6 sẽ

- Nối dây gọi API Anthropic thực trong `wiki-sync/ingest` Bước 6 (theo spec ở trên)
- Thêm dòng cost cho `claude-haiku-4-5` vào bảng giá `pre-llm-call-budget`
- Thêm skill đo lường `wiki-link-fallback-hit-rate` cho telemetry

## Khi nào dùng

- Được gọi bởi `wiki-sync/ingest/SKILL.md` Bước 6.

## Đầu vào

- `structured_content` — các thực thể + section đã trích xuất từ adapter
- `chunk_text` — văn bản đang được phân tích (mỗi chunk embedding)
- `page_id` — uuid của row `ops.knowledge_pages` đang được xây (target cho FK)

## Quy trình (Sprint 1: chỉ regex)

### Bước 1 — Pass regex

Với mỗi pattern trong `knowledge/link-inference-rules.yaml.link_types[]`:
- Áp pattern lên `chunk_text`
- Với mỗi match: INSERT vào `ops.knowledge_links` với:
  - `source_page_id` = page_id hiện tại
  - `target_page_id` = tra cứu theo slug (NULL nếu không tìm thấy; đó là tín hiệu orphan-link mà `wiki-sync/audit` tìm)
  - `link_type` = `id` của rule
  - `extracted_from_section` = (vị trí trong chunk_text nơi có match)
  - `source_text` = snippet đã match
  - `extraction_method` = 'regex'
  - `confidence` = 1.0

### Bước 2 — LLM-fallback Sprint 4 (HOÃN, stub)

Sprint 1: bước này là no-op.

Sprint 4 sẽ thêm:
- Nếu `regex_pass tạo ra < 3 link` VÀ `chunk có > 1000 token`:
- Nếu `knowledge/feature-flags.yaml#wiki-sync.llm_fallback_enabled = true`:
- Gọi LLM với template `link-extract-prompt.md`
- INSERT các link đã trích với `extraction_method='llm'`, `confidence=<llm-confidence>`

Cap chi phí: $0.05 mỗi lần gọi LLM-fallback.

## Đầu ra

```jsonc
{
  "links_created": <int>,
  "method_breakdown": { "regex": <int>, "llm": <int (Sprint 4+)> },
  "llm_cost_usd": <numeric>     // 0 in Sprint 1
}
```

## Các tình huống lỗi

| Triệu chứng | Phản ứng |
|---|---|
| Lỗi compile pattern | Bỏ qua rule đó; log cảnh báo |
| Bulk INSERT lỗi | Retry một lần; lần thứ hai lỗi, mark step state='partial' |
| Tra cứu slug target mơ hồ (nhiều match) | Chọn cái đầu; flag để audit |

## Ước lượng chi phí

Sprint 1: $0 (chỉ regex).
Sprint 4 với LLM-fallback BẬT: ~$0.01-$0.05 mỗi chunk nơi fallback kích hoạt (đa số chunk sẽ không kích hoạt).

## Phạm vi Sprint

- Sprint 1: chỉ regex (tệp này)
- Sprint 4: LLM-fallback qua feature flag

## Liên quan

- Parent: `06-ai-ops/skills/wiki-sync/ingest/SKILL.md` (Bước 6)
- Tier 1: `knowledge/link-inference-rules.yaml`
- Feature flag (Sprint 4): `knowledge/feature-flags.yaml#wiki-sync.llm_fallback_enabled`
- Bảng đồng hành: `ops.knowledge_links` (migration 00006)
