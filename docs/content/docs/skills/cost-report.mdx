---
title: 'Skill: cost-report'
description: Cost breakdown + unit economics reporting
source_path: 06-ai-ops/skills/cost-report/SKILL.md
source_hash: 990a3639e02e56a154e74802aa50eb6d6906583042bbe614c41eabf5d8160e96
generated_at: '2026-05-19T05:20:46.348Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# Cost Report

> Skill turns rows của `ops.cost_attributions` thành Telegram message founder reads ở 15 seconds.

## Khi nào sử dụng

Ba trigger contexts:

1. **Founder hỏi về cost.** `/cost`, `/cost monthly`, `/cost per-ticket`
2. **Monthly digest generation.** `monthly-learning-review` Economics section
3. **Optimization review prep.** `cost-optimization-review` skill

## Inputs

| Input | Type | Required |
|---|---|---|
| `mode` | string | yes |
| `period` | string | conditional |
| `role` | string | optional |
| `task_kind` | string | optional |
| `output_format` | string | optional |
| `compare_to_previous` | bool | optional |

## Modes

- `summary` — top-level overview
- `by_role` — per-role breakdown
- `by_task_kind` — unit economics per task
- `unit_economics` — cost-per-output metrics
- `health_score` — composite health
- `budget_status` — per-role vs cap
- `time_series` — trends

## Outputs

YAML, Telegram-formatted, hoặc markdown.

## Procedure

### Step 1 — Validate inputs

### Step 2 — Run primary query

Per mode.

### Step 3 — Compute health score

Weighted formula.

### Step 4 — Format output

Per format.

### Step 5 — Log

ops.agent_runs.

## Cost estimate

- 1-3 SQL queries
- < $0.01 per invocation
- Founder uses ~2-5x/day = $1.50/month

---

Related: `cost-optimization-review`.
