---
title: 'Skill: architect'
description: Phase 5 của CLA workflow (Bài #20)
source_path: 06-ai-ops/skills/capability-lifecycle/architect/SKILL.md
source_hash: 401d998b1d64bc5b7b80d12ca9f876701b219176d2801ff5ba948b8826fb406e
generated_at: '2026-05-19T05:20:46.332Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# Architect (CLA Phase 5)

## Khi nào sử dụng

- Sau khi Phase 4 HITL Tier B ghi nhận lựa chọn tùy chọn của người sáng lập. `ops.capability_runs.current_phase = 5`, `state_payload.selected_option_id` được đặt.
- State = `architecting`.

## Inputs

- `capability_id`
- `selected_option_id` từ `ops.capability_runs.state_payload`
- Tất cả các artifacts trước đó dưới `.archives/cla/<id>/` (problem, domain, gap, options)
- Template spec tại `wiki/capabilities/_TEMPLATE/spec.md`

## Quá trình

### Step 1 — Từ chối nếu Phase 4 thiếu

`failure_handling.phase_4_skipped` theo `flow.yaml`: nếu `.archives/cla/<id>/options.md` không tồn tại HOẶC `state_payload.selected_option_id` là null, ABORT với: "Phase 5 yêu cầu Phase 4 options.md + lựa chọn tùy chọn Tier B. Chạy Phase 4 trước."

### Step 2 — Phân tích tác động từng Bài-toán

Với mỗi 19 bài toán trước + Bài #20, phân tích tác động. Tạo một bảng; mỗi hàng là một bài toán + danh mục tác động + thay đổi yêu cầu. Bỏ qua các hàng nơi tác động là "none".

### Step 3 — Tạo `spec.md` chính thức

Đọc `wiki/capabilities/_TEMPLATE/spec.md` và điền mỗi phần.

### Step 4 — Tạo draft làm việc

Tạo trước dưới `.archives/cla/<capability_id>/draft/`.

### Step 5 — Dry-run `pnpm check`

Chạy dry-run trên draft tier1-diffs.

### Step 6 — `@cto` sanity review

Dispatch Agent call tới `@cto`.

### Step 7 — Muse panel

Gọi panel `high-stakes-decision-panel`.

### Step 8 — Ghi `ops.decisions` row

Tạo decision row trong ops.

### Step 9 — Hành thức HITL Tier C

Lệnh `/cla` gọi full Tier C flow.

### Step 10 — Persist state

Cập nhật ops.capability_runs.

## Outputs

- `.archives/cla/<capability_id>/spec.md`
- `.archives/cla/<capability_id>/draft/`
- ops.decisions + ops.hitl_runs rows

## State transition

`architecting → planning`

## HITL

**Tier C** — full ceremony.

## Failure modes

Các trường hợp thất bại khác nhau.

## LLM mode awareness

- **Subscription:** Tất cả LLM calls đi qua founder's Claude Code session.
- **Hybrid / Full API:** Autonomous run; cost cao nhất của bất kỳ phase nào.
- **Fallback:** Không thể chạy.

## Cost estimate

- Anthropic API: ~$1.00-2.00 mỗi invocation.
- Founder time: 60-90 min.
- Cost-bucket: `ai-ops-cla`.

---

**Next phase invokes:** `sprint-planner` (Phase 6).
