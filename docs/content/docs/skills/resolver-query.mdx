---
title: 'Skill: resolver-query'
description: |
  CONSUMER CONTRACT cho resolver v2 LLM-Native Catalog. Bất kỳ skill hoặc agent
  nào CÓ THỂ invoke skill này để tìm AI workforce recipients cho một trigger
  qua các kinds (skill, command, agent, mcp, persona). v2 dùng Mode A (in-session
  ambient catalog — preferred), Mode B (explicit LLM-mediated query — debug/audit),
  và Mode C (keyword fallback cho non-LLM consumers).
  Trả về: primary recipient + supporting composition + invocation spec.
  Caller chịu trách nhiệm execute invocation (D-4 INVARIANT).
source_path: 06-ai-ops/skills/resolver-query/SKILL.md
source_hash: 1cfb48d8b4020bc7c97634380dcc7595f1e995da09b5bddfa5e4f3ada96ca6aa
generated_at: '2026-05-30T16:11:31.662Z'
generated_by: docs-engine v1.2.0
category: skill
language: vi
translated: true
translated_source_hash: 1cfb48d8b4020bc7c97634380dcc7595f1e995da09b5bddfa5e4f3ada96ca6aa
translated_at: '2026-05-30T16:30:00Z'
---

{/* generated-by: docs-engine v1.2.0 */}

## Khi nào dùng skill này

Invoke từ một skill, agent, hoặc workflow khác khi bạn cần tìm đúng AI workforce
recipient cho một natural-language task — không cần grep filesystem hay hardcode
lookups.

**Ba consumer modes — chọn theo context:**

### Mode A (PREFERRED — in-session ambient catalog)

Bạn đang vận hành bên trong một Claude Code session. Catalog
`knowledge/recipients/{skills,commands,agents,personas,mcps}.md` **đã được load**
trong ambient context qua `.claude/CLAUDE.md` imports.

→ **Bạn KHÔNG cần invoke skill này.** Bạn chỉ cần reason về trigger một cách
tự nhiên và identify primary + supporting recipients từ ambient context. Sau đó
invoke recipient trực tiếp (`Skill({...})`, `Agent({...})`, v.v.).

Tùy chọn ghi audit row qua `audit.cjs` nếu quyết định quan trọng (ví dụ:
orchestration delegation).

**Latency:** 0ms thêm. **Cost:** $0 (không có LLM call thêm).

### Mode B (Explicit — operator-facing, audit/debug)

User invoked `/resolver query "<trigger>"` hoặc programmatic skill call.

Steps:
1. Load catalog (`scripts/resolver-v2/catalog-loader.cjs#loadCatalog()`)
2. Format structured prompt:
   ```
   System: <catalog như text — tất cả 5 files concatenated, hoặc kind-filtered nếu --kind cho>
   User: Cho trigger "<trigger>", return JSON:
     { primary: <recipient-id>, supporting: [<id>...], rationale: "...", alternatives: [<id>...] }
   ```
3. Current session LLM (bạn, Claude) reason → emits JSON
4. Validate IDs tồn tại trong catalog (`byId.has()`); reject hallucinations
5. Build audit record qua `audit.cjs#buildRecord` với `mode='B', llm_reasoning=<rationale>, composition_supporting=[<supporting>]`
6. Write audit qua `mcp__supabase-ops__insert` tới `ops.resolver_decisions`
7. Return human-readable output

**Latency:** ~5-500ms (catalog load + LLM reasoning). **Cost:** $0 in-session
(không separate API call); cached system prompt nếu invoked nhiều lần.

### Mode C (Keyword fallback — non-LLM consumers chỉ)

CRON jobs, Edge Functions, pre-commit hooks. Không có LLM trong loop.

Invoke `scripts/resolver-v2/keyword-fallback.cjs#match({trigger})` trực tiếp.
Single-recipient response. ~30% recall (acknowledged degradation vs Mode A).

Dùng cái này **CHỈ KHI** bạn không thể reach LLM context. Encourage migration
sang Mode A.

**Latency:** \<5ms. **Cost:** $0.

## Output schema (Mode A/B)

```typescript
{
  primary: {
    id: string,           // ví dụ "skill/customer-onboarding"
    kind: string,
    invoke: string,       // exact invocation snippet
    when_to_use: string,
    status: string,
  },
  supporting: Array<{     // composition — recipients để dùng alongside
    id: string,
    kind: string,
    invoke: string,
    why: string,          // tại sao cái này hỗ trợ primary
  }>,
  alternatives: Array<{   // other valid primary choices caller có thể prefer
    id: string,
    kind: string,
    confidence: number,   // LLM-judged 0-1
  }>,
  rationale: string,       // LLM's natural-language reasoning
  catalog_files_loaded: string[],
  mode: 'A' | 'B' | 'C',
  audit_run_id?: string,   // nếu audit được written
}
```

## INVARIANTS

**INV-1:** Zero false-positive matches. LLM hallucinations rejected.

**INV-2:** Single source of truth — chỉ `knowledge/recipients/*.md`. KHÔNG có
parallel YAML route file.

**INV-3 (D-4 carryover từ v1):** Skill này KHÔNG execute recipient.
Nó trả về metadata + invocation spec. Caller (bạn) execute.

**INV-4:** Catalog auto-sync là frontmatter→catalog (one-way). Founder
overrides qua direct edit + `<!-- override-start -->` markers được detect ở
next sync.

## Failure modes

| Code | Cause | Handling |
|---|---|---|
| `INVALID_TRIGGER` | nil/empty trigger | Throw; caller phải provide |
| `CATALOG_DOWN` | recipients/ missing | Throw; fix install |
| `CATALOG_PARSE_ERROR` | malformed markdown | Throw với file+line |
| `DUPLICATE_RECIPIENT_ID` | same id trong 2 files | Throw; manual fix |
| `MISSING_REQUIRED_FIELD` | catalog entry incomplete | Throw với entry ID |
| `HALLUCINATED_RECIPIENT` | LLM trả về unknown ID | Mode B retries 1 lần với stricter prompt |
| `AUDIT_WRITE_FAILED` | DB unavailable | Best-effort; warn nhưng không throw |

## Xem thêm

- Spec: `.archives/cla/resolver-v2/spec.md`
- Catalog files: `knowledge/recipients/*.md`
- Engine: `scripts/resolver-v2/`
- Mode C fallback: `scripts/resolver-v2/keyword-fallback.cjs`
- Audit table: `ops.resolver_decisions` (+ migration 00035)
- v1 retrospective: `wiki/capabilities/resolver/retrospective.md`
