---
title: 'Skill: docs-engine/adapters/skill-adapter'
description: |
  Adapter hiển thị tệp nguồn `06-ai-ops/skills/**/SKILL.md` thành trang tham chiếu MDX
  dưới `docs/content/skills/<path>.mdx`. Giữ nguyên YAML frontmatter
  (`name`, `description`); trích xuất heading thành Fumadocs `<Steps>` nếu phần "Process" tồn tại.
source_path: 06-ai-ops/skills/docs-engine/adapters/skill-adapter/SKILL.md
source_hash: d336466af724fb405a14e6d1c25b2b36b567c08a35597e4b3203fd834e475fad
generated_at: '2026-05-19T05:20:46.353Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# docs-engine/adapters/skill-adapter

## Input pattern

`06-ai-ops/skills/**/SKILL.md`

## Output pattern

`docs/content/skills/<path>.mdx` (path = relative path dưới `06-ai-ops/skills/` trừ `/SKILL.md`)

## Process

1. Parse YAML frontmatter (`name`, `description`).
2. Tính `source_hash = sha256(canonical(file_contents))`.
3. Hiển thị MDX với frontmatter `{ title: <name>, source_path: <path>, source_hash, generated_at, generated_by: 'docs-engine v0.1.0' }`.
4. Body: pass through markdown. Nếu phần "## Process" tồn tại, biến đổi danh sách có số thành Fumadocs `<Steps>` component.
5. Thêm marker idempotency `<!-- generated-by: docs-engine v0.1.0 -->` ở đầu body.

## Edge cases

- `SKILL.md` không có frontmatter → dẫn xuất title từ H1 đầu tiên; cờ trong adapter_errors.
- YAML `description:` multi-line — giữ nguyên định dạng.
- Sub-skills dưới wiki-sync/ — giữ nguyên cấp độ thư mục.

## HITL / Cost

Tier A. ~$0.02-0.05 mỗi tệp. Deterministic phần lớn (LLM chỉ nếu yêu cầu prose enrichment — disabled at v1.0).

## See also

- Adapter anh em: agent-adapter, hook-adapter, command-adapter, vân vân.
- Walker: `docs-engine/sync/SKILL.md`
