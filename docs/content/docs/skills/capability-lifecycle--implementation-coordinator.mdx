---
title: 'Skill: implementation-coordinator'
description: Phase 7 của CLA workflow
source_path: 06-ai-ops/skills/capability-lifecycle/implementation-coordinator/SKILL.md
source_hash: 9293b2612c37edce0f37a40fe192eb779018e582d684a33d10f2c1e36343114a
generated_at: '2026-05-19T05:20:46.339Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: "2026-05-19T15:00:00Z"
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# Implementation Coordinator (CLA Phase 7)

## Khi nào sử dụng

- Sau khi Phase 6 sprint-plan.md đã được phê duyệt.
- Trạng thái = `implementing`.
- Được kích hoạt lại bởi `/cla resume <id>`.

## Đầu vào

- `capability_id`
- `sprint_plan_path` — `.archives/cla/<id>/sprint-plan.md`
- `spec_path` — `.archives/cla/<id>/spec.md`
- `draft_root` — `.archives/cla/<id>/draft/`
- `state_payload.completed_sprints`

## Quá trình — đa phiên, kéo dài nhiều tuần

### Bước 1 — Đọc trạng thái, xác định sprint tiếp theo

Truy vấn ops.capability_runs, trích xuất trạng thái.

### Bước 2 — Đọc các sản phẩm bàn giao của sprint

Từ sprint-plan.md, trích xuất các sản phẩm bàn giao (deliverables).

### Bước 3 — Ủy thác code cho @cto

Gọi Focused Agent.

### Bước 4 — Stage + commit + PR

Git add, commit, push, tạo PR.

### Bước 5 — HITL Tier B cho mỗi PR

Founder merge thông qua GitHub.

### Bước 6 — Lưu trạng thái sprint hoàn thành

UPDATE ops.capability_runs.

### Bước 7 — Lặp lại hoặc hoàn tất

Tiếp tục các sprint hoặc hoàn tất ở trạng thái deployed.

## Khôi phục phiên đa phiên

`/cla resume <id>` đọc trạng thái, chạy kiểm tra trôi dạt ở Phase 0, tiếp tục từ Bước 2.

## Đầu ra

- N PR trong lịch sử git
- Bản nháp được commit vào các vị trí chính thức
- Dấu vết ops.events
- state_payload đã cập nhật

## Chuyển trạng thái

`implementing → deployed` (sau sprint cuối cùng + E2E).

## HITL

**Tier B cho mỗi PR**.

## Các chế độ thất bại

Sản phẩm bàn giao không có bản nháp, husky thất bại, founder yêu cầu thay đổi.

## Ước lượng chi phí

- ~$0.50-2.00 cho mỗi sprint
- Thời gian của founder: ước lượng theo từng sprint
- Cost-bucket: `<capability_id>`

---

Tiếp theo: `catalog-updater` (Phase 8).
