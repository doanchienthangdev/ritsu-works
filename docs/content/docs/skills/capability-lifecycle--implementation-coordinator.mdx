---
title: 'Skill: implementation-coordinator'
description: Phase 7 của CLA workflow
source_path: 06-ai-ops/skills/capability-lifecycle/implementation-coordinator/SKILL.md
source_hash: 9293b2612c37edce0f37a40fe192eb779018e582d684a33d10f2c1e36343114a
generated_at: '2026-05-19T05:20:46.339Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# Implementation Coordinator (CLA Phase 7)

## Khi nào sử dụng

- Sau Phase 6 sprint-plan.md approved.
- State = `implementing`.
- Re-invoked bởi `/cla resume <id>`.

## Inputs

- `capability_id`
- `sprint_plan_path` — `.archives/cla/<id>/sprint-plan.md`
- `spec_path` — `.archives/cla/<id>/spec.md`
- `draft_root` — `.archives/cla/<id>/draft/`
- `state_payload.completed_sprints`

## Quá trình — multi-session, multi-week

### Step 1 — Read state, identify next sprint

Query ops.capability_runs, extract state.

### Step 2 — Read sprint deliverables

Từ sprint-plan.md, extract deliverables.

### Step 3 — Delegate code tới @cto

Focused Agent calls.

### Step 4 — Stage + commit + PR

Git add, commit, push, PR create.

### Step 5 — HITL Tier B per PR

Founder merges via GitHub.

### Step 6 — Persist sprint completion

UPDATE ops.capability_runs.

### Step 7 — Loop or finalize

Tiếp tục sprints hoặc finalize ở deployed.

## Multi-session resume

`/cla resume <id>` reads state, runs Phase 0 drift check, resumes ở Step 2.

## Outputs

- N PRs trong git history
- Drafts committed tới canonical locations
- ops.events trail
- Updated state_payload

## State transition

`implementing → deployed` (after final sprint + E2E).

## HITL

**Tier B per PR**.

## Failure modes

Deliverable no draft, husky fails, founder requests changes.

## Cost estimate

- ~$0.50-2.00 mỗi sprint
- Founder time: per sprint estimate
- Cost-bucket: `<capability_id>`

---

Next: `catalog-updater` (Phase 8).
