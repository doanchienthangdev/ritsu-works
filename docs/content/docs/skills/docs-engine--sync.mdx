---
title: 'Skill: docs-engine/sync'
description: Walker + adapter dispatch + MDX writer
source_path: 06-ai-ops/skills/docs-engine/sync/SKILL.md
source_hash: e446c382c0e11d5dae2f69e7f4db16cd5e0e6edd7d5061bddc9510507ce6821f
generated_at: '2026-05-19T05:20:46.356Z'
generated_by: docs-engine v1.1.0
category: skill
language: vi
translated: true
translated_at: 2026-05-19T13:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}


# docs-engine/sync

## Khi nào sử dụng

- `/docs sync` command
- `/docs update <area>` (scoped variant)
- `docs-drift-nightly` cron (với `--dry-run` flag)

## Inputs

- `area` — `commands` | `skills` | `agents` | `hooks` | `charter` | `governance` | `pillars` | `sops` | `tier1` | `all`
- `dry_run` — bool
- `force` — bool

## Quá trình

Implements SOP-AIOPS-003 Steps 1-10:

1. Preflight pnpm check
2. Walk corpus per area
3. Secret redaction layer 1
4. Dispatch tới adapters
5. Secret redaction layer 2
6. 3-way diff trên marked pages
7. Write MDX tới docs/content/
8. Post-validation pnpm check
9. Emit events
10. INSERT KPI snapshot

## Outputs

- MDX files dưới `docs/content/`
- Conflict report nếu cần
- 2-3 `ops.events` rows
- 1 `ops.kpi_snapshots` row

## HITL

Tier A. Escalates to Tier B nếu cost > cap hoặc > 50% pages change.

## Failure modes

Drift, secret leak, hand-edit conflict, adapter failure, disk write failure.

## Cost estimate

- Full walk: ~$0.40-1.00
- Area-scoped: $0.05-0.40
- Cap: `docs-sync-full-walk` ≤ $1.00

---

See: `docs-engine/SKILL.md`, `SOP-AIOPS-003-docs-sync`, `wiki-sync/ingest` pattern.
