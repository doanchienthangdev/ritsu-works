---
title: 'Hook: pre-tool-customer-message'
description: ''
source_path: .claude/hooks/pre-tool-customer-message.md
source_hash: 28f90a40fd109712bae541c323a6091dc16c0dae446ccca4f623935093a2970c
generated_at: '2026-05-19T05:20:46.392Z'
generated_by: docs-engine v1.1.0
category: hook
language: vi
translated: true
translated_source_hash: 28f90a40fd109712bae541c323a6091dc16c0dae446ccca4f623935093a2970c
translated_at: '2026-05-19T15:00:00Z'
translated_by: 'Claude Opus 4.7 (Claude Code Desktop, session translation)'
---

{/* generated-by: docs-engine v1.1.0 */}


# Hook: pre-tool-customer-message

> Lưới an toàn runtime cho việc tuân thủ tiết lộ AI hướng tới khách hàng. Theo EU AI Act Điều 50, việc thiếu tiết lộ không phải vấn đề UX — đó là vi phạm pháp lý. Hook này ngăn chặn việc gửi đi các tin nhắn không tuân thủ.

## Hook làm gì

Kiểm tra mọi tool call gửi tin nhắn hướng tới khách hàng đi ra ngoài và xác minh tiết lộ AI có mặt. Nếu thiếu, escalate lên founder để xem xét hoặc chặn nếu vi phạm rõ ràng.

Skill đồng hành `ai-disclosure-check` là việc kiểm tra **chủ động** — agent gọi nó trước khi gửi. Hook này là việc kiểm tra **bắt buộc** — nó chạy bất kể agent có gọi skill hay không. Phòng thủ theo lớp: ý định của agent + cưỡng chế runtime.

## Vì sao điều này quan trọng

- **Yêu cầu pháp lý.** EU AI Act Điều 50 (có hiệu lực 2 tháng 8 năm 2026) bắt buộc tiết lộ AI trong các tương tác chatbot. Vi phạm có mức phạt lên tới 7% doanh thu toàn cầu.
- **Nền tảng niềm tin.** Khách hàng sau này phát hiện AI không được tiết lộ sẽ mất niềm tin vào thương hiệu. Chi phí danh tiếng > chi phí pháp lý.
- **Chi phí lỗi bất đối xứng.** Một tin nhắn bị chặn nhưng thực ra ổn có chi phí rất nhỏ (founder xem xét nhanh). Một tin nhắn được gửi nhưng thực ra không tuân thủ có chi phí khổng lồ (pháp lý + danh tiếng). Tối ưu cho sự bất đối xứng này.

## Logic ra quyết định

```
function decide(payload):
  message = extract_message_body(payload)
  message_type = classify_message_type(payload)  # email_first, chat_first, etc.

  # Skip non-customer-facing tools
  if not is_customer_facing_tool(payload.tool_name):
    return allow(reason="not customer-facing")

  # Run the same checks as ai-disclosure-check skill
  checks = run_disclosure_checks(message, message_type)

  # Decide based on check results
  errors = [c for c in checks if c.severity == 'error']
  warnings = [c for c in checks if c.severity == 'warning']

  if not errors and not warnings:
    return allow(reason="all disclosure checks passed")

  if errors:
    # Critical: identity question not answered (Check C)
    if any(c.code == 'IDENTITY_QUESTION_NOT_ANSWERED' for c in errors):
      alert_founder_security(payload, "AI identity question not answered in customer reply")
      return block(
        reason="identity_question_not_answered",
        log_extras={
          "errors": errors,
          "violation_type": "EU_AI_ACT_ARTICLE_50_HARD",
        }
      )

    # Critical: human impersonation
    if any(c.code == 'HUMAN_IMPERSONATION_DETECTED' for c in errors):
      alert_founder_security(payload, "Outgoing message implies human, not AI")
      return block(
        reason="human_impersonation_detected",
        log_extras={"errors": errors}
      )

    # Other errors: missing disclosure / signature / escalation path
    return escalate(
      reason="disclosure_compliance_issues",
      log_extras={
        "errors": errors,
        "warnings": warnings,
        "suggested_fixes": [c.suggested_fix for c in errors],
      }
    )

  # Warnings only: allow with logged note
  return allow(
    reason="passed with warnings",
    log_extras={"warnings": warnings}
  )
```

## Phân loại tool

Các tool hướng tới khách hàng (hook này áp dụng):

- `email_sender_transactional` (khi người nhận là khách hàng, không phải nội bộ)
- `email_sender_marketing` (luôn hướng tới khách hàng theo định nghĩa)
- `resend_*` (các cuộc gọi Resend SDK tới địa chỉ khách hàng)
- `intercom_send` (chat trong sản phẩm)
- `helpscout_send` (công cụ hỗ trợ)
- `twitter_dm`, `twitter_reply` (Twitter công khai/DM)
- `linkedin_dm`, `linkedin_reply`
- Bất kỳ tool MCP tùy chỉnh nào có `_customer_send_` trong tên

Không hướng tới khách hàng (hook này KHÔNG áp dụng):

- Telegram bot (chỉ dành cho founder)
- Các kênh Slack nội bộ
- Các bình luận PR trên GitHub
- Email nội bộ (`@ritsu.ai` tới team nội bộ)
- Ghi vào database
- Các thao tác trên hệ thống file

Logic phân loại kiểm tra:
1. Khớp pattern tên tool (các danh sách trên)
2. Domain địa chỉ người nhận (có phải `@ritsu.ai` hay bên ngoài?)
3. Các kiểm tra người nhận theo kênh (kênh Slack có công khai không? Người dùng Twitter có phải bên ngoài không?)

## Cái gì bị chặn vs escalate vs allow

| Mức độ nghiêm trọng | Quyết định | Ví dụ |
|---|---|---|
| `block` (cứng) | Hook trả về `block`, tin nhắn KHÔNG được gửi, founder được cảnh báo như sự kiện bảo mật | Né câu hỏi danh tính; giả mạo con người; phủ nhận tường minh bản chất AI |
| `escalate` | Hook trả về `escalate`, founder xem xét qua Telegram trước khi gửi | Thiếu tiết lộ trong tiếp xúc đầu tiên; thiếu chữ ký; thiếu đường dẫn escalation |
| `allow` kèm cảnh báo | Hook trả về `allow` nhưng log cảnh báo | Có tiết lộ nhưng muộn trong body (>200 ký tự); cách diễn đạt escalation không trang trọng |
| `allow` sạch | Hook trả về `allow` không có cờ | Tất cả kiểm tra đều qua |

Các trường hợp `block` cứng (né danh tính, giả mạo) không được gửi cho founder xem xét — chúng đi vào log sự kiện bảo mật. Các trường hợp này thể hiện hành vi lừa dối tích cực không bao giờ nên đến được khách hàng, kể cả với phê duyệt của founder. Founder được thông báo sau khi chặn.

## Các trường hợp test

| # | Tình huống | Kỳ vọng |
|---|---|---|
| 1 | Email trả lời đầu tiên với tiết lộ chuẩn đầy đủ + chữ ký | allow |
| 2 | Email trả lời đầu tiên, body có nội dung tốt, chữ ký thiếu | escalate |
| 3 | Email trả lời đầu tiên, không có tiết lộ AI ở đâu | escalate |
| 4 | Khách hàng hỏi "are you human?", phản hồi nói "Yes, I can help" mà không xác nhận AI | block (cứng) |
| 5 | Email body chứa "I personally feel" | block (cứng, giả mạo) |
| 6 | Email tiếp theo (không phải đầu tiên), chữ ký có mặt, không có tiết lộ tiếp xúc đầu tiên | allow (không áp dụng kiểm tra) |
| 7 | Email nội bộ tới `gps@ritsu.ai` (founder relay) | allow (không hướng tới khách hàng) |
| 8 | Twitter DM với tiết lộ trong tin nhắn chat đầu tiên | allow |
| 9 | Twitter trả lời công khai, không có tiết lộ (giả định phạm vi hạn chế) | escalate (cảnh báo, khách hàng không thấy context thread) |
| 10 | Email tiếng Việt đầu tiên với tiết lộ tiếng Việt phù hợp | allow |
| 11 | Email tiếng Việt đầu tiên với tiết lộ tiếng Anh (locale không khớp) | escalate (cảnh báo) |
| 12 | Có tiết lộ nhưng ở đoạn thứ 5 của email dài | escalate (vị trí yếu) |
| 13 | Email marketing blast tới subscribers, mọi thứ ổn | allow (theo chính sách đây cũng là Tier C+ qua pre-tool-publish) |
| 14 | Founder đích thân gửi (role founder) | allow (founder là người, không có khai báo AI) |

## Hook kết hợp với các hook khác

Hook này kết hợp với:

- **`pre-tool-publish`** (Bài #2) — cũng chạy trên các tool gửi hướng tới khách hàng. Cả hai hook đều phải qua:
  - `pre-tool-publish` kiểm tra: phân loại tier, yêu cầu dry-run, số người nhận, luồng phê duyệt founder
  - `pre-tool-customer-message` kiểm tra: tuân thủ tiết lộ AI
  - Nếu một trong hai nói block, tin nhắn bị chặn.
  - Nếu cả hai nói escalate, một lần phê duyệt founder bao trùm cả hai.

- **`pre-tool-secrets`** (Bài #2) — chạy đầu tiên để xác minh role có thể dùng token email/mạng xã hội. Nếu thất bại, tin nhắn không bao giờ đến được hook này.

- **`pre-delegate-check`** (Bài #5) — chỉ mang tính thông tin, không khống chế hook này.

## Ghi chú về hiệu năng

- Các kiểm tra tiết lộ phần lớn là regex trên body (nhanh, \< 50ms)
- Các kiểm tra tiếng Việt là pass thứ hai trên body được dịch/gắn tag locale (\< 50ms)
- Phát hiện câu hỏi danh tính quét tin nhắn cuối cùng của user trong context hội thoại (nếu có sẵn, \<100ms)
- Mục tiêu tổng: p95 \< 200ms

## Khả năng quan sát

Mỗi lần escalate/block tạo ra:

- Entry trong `ops.agent_runs.hook_events` với kết quả kiểm tra đầy đủ
- Đối với block: cảnh báo Telegram tới founder với mức độ nghiêm trọng
- Đối với escalate: yêu cầu phê duyệt Telegram với các đề xuất sửa được render trước

Rà soát hàng tháng (`monthly-learning-review`, Bài #4) tổng hợp:
- % tin nhắn hướng tới khách hàng có tiết lộ phù hợp ngay lần thử đầu tiên
- Các lý do escalate phổ biến nhất (cơ hội đào tạo)
- Xu hướng tỷ lệ block (nên gần bằng 0 nếu các agent được prompt tốt)

Nếu tỷ lệ block vượt 1% khối lượng hướng tới khách hàng, audit:
- Các system prompt của agent có rõ ràng về tiết lộ không?
- Các skill `support-reply-drafting` v.v. có gọi `ai-disclosure-check` đúng cách không?
- Tài liệu brand voice có đủ rõ ràng không?

## Hook audit tuân thủ

Một job lên lịch riêng (sau Phase D) truy vấn `ops.agent_runs.hook_events`:

```sql
SELECT
  DATE_TRUNC('week', ts) AS week,
  COUNT(*) FILTER (WHERE decision = 'block') AS blocks,
  COUNT(*) FILTER (WHERE decision = 'escalate') AS escalates,
  COUNT(*) FILTER (WHERE decision = 'allow') AS allows,
  COUNT(*) AS total
FROM ops.agent_runs
JOIN LATERAL (
  SELECT (jsonb_array_elements(hook_events) ->> 'hook_name') AS hook_name,
         (jsonb_array_elements(hook_events) ->> 'decision') AS decision
) h ON h.hook_name = 'pre-tool-customer-message'
WHERE ts >= NOW() - INTERVAL '30 days'
GROUP BY DATE_TRUNC('week', ts)
ORDER BY week;
```

Mục tiêu tuân thủ: tỷ lệ allow 99%+ với tất cả kiểm tra `ok`. Nếu tỷ lệ allow rơi xuống dưới 95%, escalate lên founder như một vấn đề quy trình.

## Tham chiếu triển khai

```python
import re

DISCLOSURE_PATTERNS_EN = [
    re.compile(r"I'?m Ritsu Assistant\s*[—,]\s*an? AI system", re.I),
    re.compile(r"I'?m Ritsu Assistant,? an AI helper", re.I),
]
DISCLOSURE_PATTERNS_VI = [
    re.compile(r"M[ìi]nh là Ritsu Assistant\s*[—,]?\s*một hệ thống AI", re.I),
    re.compile(r"T[ôo]i là Ritsu Assistant,? một hệ thống trí tuệ nhân tạo", re.I),
]

IDENTITY_QUESTIONS_EN = [
    re.compile(r"\bare you (an? )?(AI|bot|robot|human|real|person)\b", re.I),
    re.compile(r"\bis this (a |an )?(real|live) (person|human)\b", re.I),
]
IDENTITY_QUESTIONS_VI = [
    re.compile(r"b[ạa]n l[àa] (AI|robot)( à)?\??", re.I),
    re.compile(r"có ph[ảa]i (robot|người th[ậâ]t)", re.I),
]

IMPERSONATION_PATTERNS = [
    re.compile(r"\bI personally (feel|think|believe)\b", re.I),
    re.compile(r"\bWhen I was a (student|kid|child|teenager)\b", re.I),
    re.compile(r"\bMình c[ũu]ng l[àa] sinh viên\b", re.I),
    # ... more patterns from voice doc
]

def decide(payload):
    if not is_customer_facing(payload):
        return {'decision': 'allow', 'reason': 'not customer-facing'}

    body = extract_body(payload)
    msg_type = classify_message_type(payload)
    locale = detect_locale(body, payload)

    errors = []
    warnings = []

    # Check A: first-contact disclosure
    if msg_type.endswith('_first'):
        patterns = DISCLOSURE_PATTERNS_VI if locale == 'vi' else DISCLOSURE_PATTERNS_EN
        if not any(p.search(body[:500]) for p in patterns):
            if any(p.search(body) for p in patterns):
                warnings.append({'code': 'DISCLOSURE_LATE_PLACEMENT'})
            else:
                errors.append({'code': 'MISSING_FIRST_CONTACT_DISCLOSURE'})

    # Check C: identity question response
    prev_msg = get_previous_customer_message(payload)
    if prev_msg:
        questions = IDENTITY_QUESTIONS_VI + IDENTITY_QUESTIONS_EN
        if any(p.search(prev_msg) for p in questions):
            patterns = DISCLOSURE_PATTERNS_VI + DISCLOSURE_PATTERNS_EN
            if not any(p.search(body[:300]) for p in patterns):
                errors.append({'code': 'IDENTITY_QUESTION_NOT_ANSWERED'})

    # Check E: human impersonation
    for p in IMPERSONATION_PATTERNS:
        if p.search(body):
            errors.append({'code': 'HUMAN_IMPERSONATION_DETECTED', 'pattern': p.pattern})
            break

    # ... Checks B (signature) and D (escalation path) similar

    # Decide
    hard_blocks = [e for e in errors if e['code'] in
                   ('IDENTITY_QUESTION_NOT_ANSWERED', 'HUMAN_IMPERSONATION_DETECTED')]
    if hard_blocks:
        alert_security(payload, hard_blocks)
        return {'decision': 'block',
                'reason': hard_blocks[0]['code'],
                'log_extras': {'errors': errors}}

    if errors:
        return {'decision': 'escalate',
                'reason': 'disclosure_compliance_issues',
                'log_extras': {'errors': errors, 'warnings': warnings}}

    return {'decision': 'allow',
            'log_extras': {'warnings': warnings} if warnings else {}}
```

## Các chế độ thất bại

- **Hook lỗi / dependency không khả dụng** — fail-closed (chặn). Tin nhắn được giữ lại, founder được cảnh báo để điều tra. Tốt hơn là trì hoãn việc gửi còn hơn là gửi đi tin nhắn không tuân thủ.
- **Trích xuất body thất bại** — `block` với lý do `body_extraction_failed`. Founder điều tra payload.
- **Phát hiện locale sai** — các kiểm tra có thể áp dụng pattern sai. Giảm thiểu: nếu locale mơ hồ, chạy CẢ kiểm tra tiếng Anh và tiếng Việt. Nếu cả hai đều không qua, escalate.

## Khi nào hook này thay đổi

Các yếu tố kích hoạt việc xem xét lại:
- Yêu cầu pháp lý mới (luật AI Việt Nam được thông qua; California cập nhật SB 1001; v.v.)
- Kênh hướng tới khách hàng mới được thêm (ví dụ: giọng nói — cần kiểm tra tiết lộ bằng giọng nói)
- Hỗ trợ ngôn ngữ mới — thêm pattern
- Tỷ lệ false positive vượt 5% — tinh chỉnh pattern

Mỗi thay đổi là PR D-Std theo `governance/HITL.md` (đây là hook an toàn quan trọng).

---

*Chi phí của một tin nhắn không tuân thủ không phải là tin nhắn tiếp theo — đó là 10.000 tin nhắn tiếp theo, cuộc điều tra pháp lý, câu chuyện trên báo chí. Hook này làm cho sự bất đối xứng chi phí trở nên hiển thị và hành động theo nó.*

