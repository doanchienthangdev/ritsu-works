---
title: 'Hook: pre-llm-call-budget'
description: ''
source_path: .claude/hooks/pre-llm-call-budget.md
source_hash: fa76d4ef92d62f5d92067f89253f4f1a38faf7f143e54f0fbf8a93d808fc3430
generated_at: '2026-05-19T05:20:46.390Z'
generated_by: docs-engine v1.1.0
category: hook
language: vi
translated: true
translated_source_hash: fa76d4ef92d62f5d92067f89253f4f1a38faf7f143e54f0fbf8a93d808fc3430
translated_at: '2026-05-19T12:34:00.000Z'
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}

# Hook: pre-llm-call-budget

> Runtime enforcement của `knowledge/economic-architecture.md` Axis 2. Mỗi LLM call đi qua đây. Ba-tier escalation (alert 80% / escalate 100% / block 150%) bảo vệ chống silent cost spirals khi không blocking legitimate work.

## Những gì nó làm

Cho mỗi LLM call được attempted:

1. Compute current month's cost cho calling role
2. Compare tới role's `economic_budget.monthly_cap_usd`
3. Decide: allow / alert / escalate / block dựa trên ratio
4. Cũng check per-task-kind soft caps (catches single-task overruns)
5. Write một `cost_attribution` row sau call completes
6. Log alerts/escalations/blocks vào `ops.budget_alerts`

Đây là **runtime** enforcement layer. **Proactive** companion là `cost-report` skill (founder hỏi). Defense in depth: visibility + enforcement.

## Tại sao điều này là critical

Per `knowledge/economic-architecture.md` "failure mode #1 — silent cost spiral": một bug trong skill loops, $200 disappears overnight, founder discovers tiếp theo billing cycle. Hook này là structural prevention.

Nếu không có hook này:
- `cost-report` chỉ show past damage
- Per-role budgets trong ROLES.md là aspirational
- 3-tier escalation được documented nhưng không enforced

## Logic quyết định

```
function decide(payload):
  # Identify role + task_kind
  role = payload.agent_role
  task_kind = extract_task_kind(payload)  # từ current task's state_payload

  # Skip non-billable
  if role == 'founder':
    return allow(reason="founder calls — manual oversight by definition")

  # Lookup budget
  role_def = load_role_cached(role)
  econ = role_def.get('economic_budget', {})
  monthly_cap = econ.get('monthly_cap_usd', 0)

  if monthly_cap == 0:
    # No budget set → tracking only, no enforcement
    return allow(reason="no monthly_cap_usd set — tracking only")

  # Pull current month's spend (cached 30s)
  current_cost = get_role_monthly_cost(role)
  ratio = current_cost / monthly_cap

  alert_pct = econ.get('alert_at_pct', 0.80)
  escalate_pct = econ.get('escalate_at_pct', 1.00)
  block_pct = econ.get('hard_block_at_pct', 1.50)

  # Check role monthly cap
  if ratio >= block_pct:
    write_alert('hard_block_150', role, current_cost, monthly_cap, ratio)
    return block(
      reason=f"role {role} at {ratio:.0%} of monthly budget — hard ceiling",
      log_extras={
        "current_cost_usd": current_cost,
        "monthly_cap_usd": monthly_cap,
        "ratio": ratio,
        "to_unblock": "PR raising economic_budget.monthly_cap_usd in ROLES.md (Tier C)"
      }
    )

  if ratio >= escalate_pct:
    write_alert_if_first('escalate_100', role, current_cost, monthly_cap, ratio)
    return escalate(
      reason=f"role {role} at {ratio:.0%} of monthly budget — founder approval required",
      log_extras={
        "current_cost_usd": current_cost,
        "monthly_cap_usd": monthly_cap,
        "ratio": ratio,
        "estimated_call_cost": estimate_call_cost(payload),
        "founder_options": ["approve_this_call", "raise_budget", "deny"]
      }
    )

  # Check per-task-kind soft cap (independent of monthly)
  if task_kind:
    tk_caps = econ.get('per_task_kind_caps', {})
    tk_cap = tk_caps.get(task_kind)
    if tk_cap:
      current_task_cost = get_current_task_cost(payload.task_id)
      estimated_call = estimate_call_cost(payload)
      if (current_task_cost + estimated_call) > tk_cap * 2:  # 2x cap = hard escalate
        return escalate(
          reason=f"task instance projected to exceed 2x soft cap for {task_kind}",
          log_extras={
            "task_kind_cap_usd": tk_cap,
            "current_task_cost_usd": current_task_cost,
            "estimated_next_call_usd": estimated_call,
            "projected_total_usd": current_task_cost + estimated_call,
          }
        )

  if ratio >= alert_pct:
    write_alert_if_first('warning_80', role, current_cost, monthly_cap, ratio)
    return allow(
      reason="80% threshold reached — informational",
      log_extras={
        "delegation_signal": "budget-warning-80",  # consistent with Bài #5 hook signals
        "current_cost_usd": current_cost,
        "monthly_cap_usd": monthly_cap,
        "ratio": ratio,
      }
    )

  return allow(reason=f"healthy: {ratio:.0%} of budget")
```

## Sau call: write cost attribution

Hook fires `pre-tool`, nhưng cũng có `post-tool` companion mà writes actual cost:

```
function post_tool(payload):
  # Extract token counts từ response
  usage = payload.response.usage
  cost = calculate_cost(
    model=payload.request.model,
    input_tokens=usage.input_tokens,
    cache_creation=usage.cache_creation_input_tokens,
    cache_read=usage.cache_read_input_tokens,
    output_tokens=usage.output_tokens,
    service_tier=payload.request.service_tier or 'standard',
  )

  insert_cost_attribution(
    run_id=payload.run_id,
    task_id=payload.task_id,
    agent_role=payload.agent_role,
    task_kind=extract_task_kind(payload),
    model=payload.request.model,
    service_tier=payload.request.service_tier or 'standard',
    input_tokens=usage.input_tokens,
    cache_creation_tokens=usage.cache_creation_input_tokens,
    cache_read_tokens=usage.cache_read_input_tokens,
    output_tokens=usage.output_tokens,
    cost_usd=cost,
    ts=now()
  )

  # Invalidate cache cho next pre-tool call
  invalidate_role_cost_cache(payload.agent_role)
```

Điều này conceptually là một hook với cả `pre` và `post` halves; framework permitting, implement như paired hooks.

## Bảng định giá

Cached locally; refreshed monthly via PR tới `knowledge/pricing/anthropic.yaml`. Table bao gồm:

```yaml
# Per million tokens, in USD. Updated 2026-05-02.
models:
  claude-opus-4-7:
    input: 15.00
    output: 75.00
    cache_creation: 18.75
    cache_read: 1.50
  claude-sonnet-4-6:
    input: 3.00
    output: 15.00
    cache_creation: 3.75
    cache_read: 0.30
  claude-haiku-4-5:
    input: 0.80
    output: 4.00
    cache_creation: 1.00
    cache_read: 0.08
service_tier_multipliers:
  standard: 1.0
  batch: 0.5
  priority: 1.5    # placeholder; Priority pricing varies
```

Nếu model không tìm trong table → log warning, sử dụng Opus rates như conservative fallback (tốt hơn overestimate hơn under-track).

## Ghi chú hiệu năng

- Role budget lookup: cached per-process per-role với 30-second TTL
- Current cost calculation: cached 30 seconds; invalidated bởi post-tool writes
- Pricing table: cached cho entire session
- Target: p95 \< 50ms cho pre-tool half

Cho high-volume roles (100+ calls/min), 30-second cache là essential. Cache misses làm single SQL query (`SELECT SUM(cost_usd) FROM ops.cost_attributions WHERE agent_role = X AND ts >= month_start`).

## Các trường hợp kiểm thử

| # | Scenario | Expected pre-tool decision |
|---|---|---|
| 1 | Fresh month, $0 spent, simple call | allow (healthy) |
| 2 | Role ở 75% của monthly cap | allow (healthy) |
| 3 | Role ở 80% của cap, first time today | allow + warning_80 alert |
| 4 | Role ở 85% của cap, second call sau warning | allow (warning already fired this month) |
| 5 | Role ở 100% của cap | escalate |
| 6 | Role ở 100%, founder approved last call, this là next | escalate again (each call là own approval) |
| 7 | Role ở 150% của cap | block |
| 8 | Role ở 200% somehow | block (treat như 150%) |
| 9 | Founder calling | allow (no role budget cho founder) |
| 10 | Role với no economic_budget set | allow (tracking-only mode) |
| 11 | Task instance estimated to exceed 2x its task_kind cap | escalate |
| 12 | Pricing table missing cho new model | allow + warning logged (use fallback rate) |
| 13 | Hook itself fails (DB unreachable) | block (fail-closed) |
| 14 | Calibration period (first 14 days post-launch) | allow với warning ngay cả ở 100% (deferred enforcement) |

## Calibration mode

Cho first 14 days sau v1.0 launch (per `knowledge/economic-architecture.md` "30-day calibration discipline"), hook chạy ở calibration mode:

- Tất cả decisions trả về `allow` bất kể ratio
- Alerts vẫn fire (founder gets visibility)
- `ops.budget_alerts` rows vẫn inserted (data cho tuning)

Calibration mode được set qua global flag trong `knowledge/manifest.yaml` `economic_calibration_mode: true`. Founder PR flips tới `false` sau day 14 để activate full enforcement.

## Composition với other hooks

Hook này composes với:

- **`pre-tool-secrets`** (Bài #2) — chạy first; nếu API key không allowed, request không bao giờ reach budget check
- **`pre-delegate-check`** (Bài #5) — informational soft signals; không gate
- **`pre-tool-customer-message`** (Bài #6) — cho customer-facing sends; hook này composes với nó (both phải pass)
- **`pre-edit-tier1`** (Bài #2) — orthogonal (file edits, không LLM calls)

Nếu multiple hooks trả về `escalate`, founder gets một consolidated approval prompt.

## Quan sát

Mỗi escalate/block writes tới `ops.budget_alerts`. Monthly review queries:

```sql
SELECT alert_kind,
       COUNT(*) AS count,
       array_agg(DISTINCT agent_role) AS affected_roles
FROM ops.budget_alerts
WHERE ts >= now() - interval '30 days'
GROUP BY alert_kind
ORDER BY alert_kind;
```

Healthy operation:
- 0-3 `warning_80` alerts/month (some roles approaching budget)
- 0 `escalate_100` ideally (hoặc rare cho legitimate spikes)
- 0 `hard_block_150` (bất kỳ cái gì ở đây = investigation)

Nếu `hard_block_150` fires, đây là SECURITY EVENT — alert founder immediately và don't auto-resolve.

## Compliance audit

Quarterly audit (per `governance/SECRETS.md` cadence):

1. Reconcile `ops.cost_attributions` total vs Anthropic Usage Admin API cho past quarter
2. Discrepancy > 5% → investigate (untracked usage, hook bypass, ETL bug)
3. Verify tất cả roles có `economic_budget.monthly_cap_usd` set trong ROLES.md
4. Verify pricing table updated trong last 60 days

## Tham chiếu triển khai

```python
import time
from datetime import datetime

# In-process caches
_role_def_cache = {}
_role_cost_cache = {}  # {role: (cost, expiry_ts)}
_pricing_table = None

PRICING_PATH = '/path/to/knowledge/pricing/anthropic.yaml'

def load_pricing():
    global _pricing_table
    if _pricing_table is None:
        _pricing_table = yaml.safe_load(open(PRICING_PATH))
    return _pricing_table

def calculate_cost(model, input_tokens, cache_creation, cache_read,
                   output_tokens, service_tier='standard'):
    pricing = load_pricing()
    rates = pricing['models'].get(model)
    if not rates:
        # Fallback tới Opus rates (conservative)
        rates = pricing['models']['claude-opus-4-7']
        log_warning(f"Unknown model {model}, using Opus rates")

    multiplier = pricing['service_tier_multipliers'].get(service_tier, 1.0)
    cost = (
        (input_tokens / 1_000_000 * rates['input'])
      + (cache_creation / 1_000_000 * rates['cache_creation'])
      + (cache_read / 1_000_000 * rates['cache_read'])
      + (output_tokens / 1_000_000 * rates['output'])
    ) * multiplier
    return round(cost, 6)

def get_role_monthly_cost(role):
    now = time.time()
    if role in _role_cost_cache:
        cost, expiry = _role_cost_cache[role]
        if now < expiry:
            return cost

    # Cache miss
    month_start = datetime.utcnow().replace(day=1, hour=0, minute=0, second=0)
    cost = sql_scalar(
        "SELECT COALESCE(SUM(cost_usd), 0) FROM ops.cost_attributions "
        "WHERE agent_role = $1 AND ts >= $2",
        role, month_start
    )
    _role_cost_cache[role] = (float(cost), now + 30)
    return float(cost)

# ... rest per decision logic above
```

## Khi hook này changes

Triggers để revisit:

- Anthropic releases new pricing structure → update pricing table (PR)
- New model launches → add tới pricing table
- Calibration data shows defaults wrong → adjust thresholds (D-Std PR per HITL)
- New role added → economic_budget set trong ROLES.md (covered bởi add-new-role recipe)
- Currency changes → multi-currency support (post-v1.0)

Hook spec changes là D-Std PR (đây là critical safety infrastructure).

---

*Silent cost spirals là most expensive failure mode của AI workforces. Hook này là structural prevention — mỗi call gates through, mỗi dollar attributed, three thresholds catching three different shapes của overrun.*
