---
title: 06-ai-ops — Pillar AI Operations
description: ''
source_path: 06-ai-ops/README.md
source_hash: 0a5a2a6f6f154559c92359bce220496e104426f34ac34ee4a79aa93804ab3b13
generated_at: '2026-05-19T05:20:46.419Z'
generated_by: docs-engine v1.1.0
category: pillar
language: vi
translated: true
translated_at: 2026-05-19T15:00:00Z
translated_by: "Claude Opus 4.7 (Claude Code Desktop, session translation)"
---

{/* generated-by: docs-engine v1.1.0 */}

# 06-ai-ops — Pillar AI Operations

> Bản thân OS: SOP engine, MCP tooling, hook, ngân sách chi phí, episodic memory, thư viện skill, tính nhất quán cross-tier. Đây là infrastructure cho phép lực lượng AI workforce của mọi pillar khác thực sự chạy được.

**Layer:** Evergreen
**Stage status:** DEEP (alive — đã build một phần: `skills/`, `sops/`, `sop-engine/`)
**Pillar code:** AIOPS
**Owner role(s):** aiops-engineer, etl-runner, code-reviewer
**HITL baseline:** B (ETL thường lệ = B; thay đổi schema/runtime = C; chạm đến secret = D)

---

## Phạm vi

Pillar này sở hữu **infrastructure cho AI workforce**:

- **SOP engine** — cách một `flow.yaml` trở thành SOP run thực tế (đặc tả contract, runtime, validator)
- **Skill library** — các đơn vị suy luận có thể compose (`skills/`)
- **MCP tooling** — kết nối hệ thống bên ngoài (Stripe, Telegram, GitHub, Supabase)
- **Hooks enforcement** — HITL gate, budget gate, cô lập role
- **Cost budget architecture** — caps theo role, caps mềm theo task, đối chiếu
- **Episodic memory** — đường ống `ops.run_summaries` + `ops.corrections` (Strategy E)
- **Cross-tier consistency** — runtime invariant `check-drift`
- **Integrations** — wrapper MCP cho Stripe/Telegram/GitHub/Supabase (đã merge từ `08-integrations` trước đây)

Pillar này KHÔNG sở hữu:
- Logic nghiệp vụ domain (mỗi pillar chức năng sở hữu riêng)
- Định nghĩa KPI hoặc dashboard → `10-metrics/`
- HITL POLICY (đó là `governance/HITL.md`); AI-Ops sở hữu cơ chế ENFORCEMENT

## Sub-pillars (build DEEP ngay bây giờ)

```
06-ai-ops/
├── sop-engine/                       # NEW v1.0.1 — load-bearing
│   ├── SOP-AIOPS-003-sop-runtime-contract/  # Schema + validator (alive)
│   └── SOP-AIOPS-004-flow-yaml-smoke-test/  # CI gate (alive)
├── skill-library/                    # → existing skills/ dir
├── mcp/                              # MERGED v1.0.1 (was mcp-tooling + 08-integrations-as-mcp)
│   ├── server-management/
│   ├── per-pillar-server-grants/
│   ├── stripe-mcp-readonly/             # Folded from old 08-integrations
│   ├── github-mcp-scoped/
│   ├── telegram-mcp/
│   └── supabase-ops-mcp/
├── hooks-enforcement/                # → .claude/hooks/ implementations
├── cost-budget-architecture/         # Per knowledge/economic-architecture.md
├── episodic-memory-architecture/     # Per knowledge/memory-architecture.md
├── cross-tier-consistency/           # Extends current check-drift
└── workforce-personas/               # NEW v1.0.2 — C-suite façade layer (CEO/CTO/CGO/CPO Phase 1)
```

## Thứ tự sub-pillar (narrative)

1. **sop-engine** — runtime contract chịu tải + flow-yaml validator
2. **skill-library** — các skill có thể compose mà agent gọi (hiện tại sống tại `skills/`)
3. **mcp** — kết nối hệ thống bên ngoài (đã merge từ mcp-tooling + integrations-as-mcp cũ)
4. **hooks-enforcement** — HITL/budget/secret gate (triển khai tại `.claude/hooks/`)
5. **cost-budget-architecture** — caps theo role, caps mềm theo task, đối chiếu
6. **episodic-memory-architecture** — đường ống `ops.run_summaries` + `ops.corrections`
7. **cross-tier-consistency** — runtime invariant `check-drift` (khóa các quy ước như đánh số sub-pillar)
8. **workforce-personas** — lớp façade cấp C-suite (CEO/CTO/CGO/CPO Phase 1); ràng buộc personas với role qua `knowledge/workforce-personas.yaml`

**Các tài sản hiện có được bảo lưu qua việc đổi tên:**
- `skills/` — các skill có thể compose mà agent gọi (`episodic-recall`, v.v.)
- `sops/SOP-AIOPS-001-capability-lifecycle/` và `sops/SOP-AIOPS-002-cross-tier-consistency/` — SOP tiền-v1.0.1; được đánh dấu để migration sang flow-schema.yaml (TODOS.md)

## SOPs

Hai namespace cùng tồn tại (chuyển tiếp):
1. **Namespace flat legacy:** `sops/SOP-AIOPS-NNN-<slug>/` (hiện có, có trước v1.0.1)
2. **Namespace sub-pillar:** `<sub-pillar>/SOP-AIOPS-NNN-<slug>/` (v1.0.1 trở đi)

SOP mới đi vào namespace sub-pillar. SOP legacy sẽ được migrate theo TODOS.md "Migrate 2 pre-contract SOPs to flow-schema.yaml conformance."

## Agents

- `aiops-engineer` (home) — build + bảo trì OS
- `etl-runner` (home) — người giữ duy nhất `SUPABASE_PRODUCT_READONLY_ETL_KEY`; chạy ETL theo lịch
- `code-reviewer` (home) — review PR khắp repo, áp dụng cho thay đổi OS

## KPI sở hữu

- Tỷ lệ thành công SOP (% run hoàn thành vs fail)
- Xu hướng chi phí agent-run (theo role, theo task-kind, theo tháng)
- Tỷ lệ recall hit (truy vấn `ops.run_summaries` vs tổng recall)
- Tỷ lệ hook chặn (kèm lý do — HITL block, budget block, secret-firewall block)
- Tỷ lệ pass drift-check (`check-drift` chạy theo thời gian)
- Độ tươi ETL (lag của `metrics.product_dau_snapshot` so với product DB)

## Phụ thuộc

- **Composes from:** none (lớp nền tảng)
- **Composed by:** mọi pillar khác (tất cả chúng chạy trên infrastructure này)

## Cô lập trọng yếu

Pillar này LÀ DUY NHẤT giữ:
- `SUPABASE_PRODUCT_READONLY_ETL_KEY` (qua role `etl-runner`)
- Credentials quản lý MCP server
- Cấp quyền cross-pillar

Sự cô lập này được thực thi qua `.claude/hooks/secret-access-firewall` (đã lên kế hoạch, xem `hooks-enforcement/`). Các role khác yêu cầu dữ liệu thông qua các bảng `metrics.*` được ETL populate; truy cập product DB trực tiếp bị cấm.

## HITL baseline

- ETL run thường lệ: Tier A (được log, không gate)
- Schema migration: Tier C
- Thêm MCP server mới: Tier C
- Thêm secret mới: Tier D-Std
- Tắt bất kỳ safety hook nào: Tier D-MAX

## Điều kiện vào (entry conditions)

Đã DEEP. Việc tách sub-pillar tiếp theo sẽ diễn ra tự nhiên (vd: sub-pillar `08-evals` khi framework eval LLM landing).

## Capability lifecycle (Bài #20)

Pillar này host master SOP cho việc thêm bất kỳ capability mới nào vào Agent OS cộng với các sub-flow tiến hóa v1.1:

- **Front-end:** `/cla` slash command — `.claude/commands/cla.md`
- **Subagent:** biến thể `@cla` giữa cuộc trò chuyện — `.claude/agents/cla.md`
- **SOPs (creation):** [`SOP-AIOPS-001-capability-lifecycle/`](sops/SOP-AIOPS-001-capability-lifecycle/)
- **SOPs (evolution v1.1):** [`SOP-AIOPS-001-fix/`](sops/SOP-AIOPS-001-fix/), [`-extend/`](sops/SOP-AIOPS-001-extend/), [`-revise/`](sops/SOP-AIOPS-001-revise/), [`-tune/`](sops/SOP-AIOPS-001-tune/), [`-deprecate/`](sops/SOP-AIOPS-001-deprecate/)
- **Skills:** [`skills/capability-lifecycle/`](skills/capability-lifecycle/) (8 phase skills + 2 v1.1 helpers: `dependency-scanner`, `version-bumper`)
- **Routing:** [`knowledge/cla-routing-keywords.yaml`](../knowledge/cla-routing-keywords.yaml) (CxO dispatch by domain)
- **Catalog:** [`wiki/capabilities/CATALOG.md`](../wiki/capabilities/CATALOG.md)
- **Playbook:** [`knowledge/phase-a2-extensions/bai-20-capability-lifecycle-DRAFT.md`](../knowledge/phase-a2-extensions/bai-20-capability-lifecycle-DRAFT.md)
- **DB:** `ops.capability_runs` + `ops.v_capability_pipeline` (creation) + `ops.v_capability_lineage` (evolution v1.1) + helper functions `capability_acquire_update_lock` / `capability_release_update_lock`

### Sử dụng `/cla propose` cho NEW capabilities (v1.0)

Workflow 8 phase xử lý drift pre-flight, đóng khung vấn đề, phân tích domain, kiểm kê hệ thống, các phương án + đề xuất, đặc tả kiến trúc (Tier C HITL với @cto + Muse panel), lập kế hoạch sprint, triển khai đa session, và promotion vào registry.

### Sử dụng `/cla {fix,extend,revise,tune,deprecate}` cho EVOLUTION (v1.1)

Sub-flow đúng kích thước cho các capability đang vận hành:

| Sub-flow | Khi nào | HITL | Chi phí |
|---|---|---|---|
| `fix` | Sửa bug, không đổi spec | B mỗi PR | ~$0.50 |
| `extend` | Mở rộng phạm vi | B → auto-C nếu đáng kể | ~$1.50 |
| `revise` | Sửa lại kiến trúc | C (luôn luôn, đầy đủ nghi thức) | ~$3-5 |
| `tune` | Tinh chỉnh lại KPI | B mỗi PR | ~$0.10 |
| `deprecate` | Sunset capability | C (không thể đảo ngược) | ~$0.30 |

Cộng thêm `/cla history <id>` (timeline chỉ-đọc) và `/cla force-unlock <id>` (phá khóa Tier D-Std).
