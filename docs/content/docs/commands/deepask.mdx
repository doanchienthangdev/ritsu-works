---
title: 'Command: /deepask'
description: >-
  Federated retrieval + capability-execution + cited synthesis over the ENTIRE internal IA.
  Zero-routing pure consumer of resolver-plan. Decompose → resolve → execute/fan-out → synthesize →
  completeness-critic → format. Returns a Pyramid, 100%-cited, authority-ranked, conflict-aware,
  freshness-tagged answer in 1 of 12 formats, OR an honest no-coverage gap + remedy. Internal-first
  (web leg delegated to deep-research). Tier A runtime (Tier-B+ legs surfaced, never auto-run).
source_path: .claude/commands/deepask.md
source_hash: 23ccd2f8b67383255617da8f16a76a909c2fb6694835c5c8cf5ac3543f066a73
generated_at: '2026-05-30T16:11:31.698Z'
generated_by: docs-engine v1.2.0
category: command
language: vi
translated: true
translated_source_hash: 23ccd2f8b67383255617da8f16a76a909c2fb6694835c5c8cf5ac3543f066a73
translated_at: '2026-05-30T16:30:00Z'
---

{/* generated-by: docs-engine v1.2.0 */}

> Orchestrator mỏng cho bộ skill `deepask/*` (`06-ai-ops/skills/deepask/`). Sở hữu phân tích cờ; ủy thác TẬT CẢ routing cho `resolver-plan` (deepask không có routing của riêng nó — không có song song source-map, bao giờ). Spec khả năng: `wiki/capabilities/deepask/spec.md` (mục tiêu Phase-8 promotion; hiện tại `.archives/cla/deepask/spec.md`). Quyết định Tier C `ops.decisions[861f8eb4-6572-4df5-b1cc-44aac0a7b014]`.

## Trạng thái triển khai (xây dựng dần theo sprint-plan.md)

| Sprint | Trạng thái | Bề mặt |
|---|---|---|
| S1 | merged (#165) | command + `orchestrator` (resolver-budget accountant) + `decompose` + `execute` (READ-only legs) + migration 00045 |
| S2 | merged (#166) | `synthesize` (Pyramid + citation guardrail + authority + conflict + freshness + adversarial-verify) + `completeness-critic` (coverage matrix + MECE + live-probe + honest-gap verdict) |
| S3 | merged (#167) | `execute` capability-RUN leg via `capability-gate.cjs` (Tier-A auto / Tier-B+ surface / D-MAX refuse) + gbrain-cap + `deep-research` delegation → **full 5-stage loop end-to-end** |
| S4 | merged (#168) | Format Engine (`deepask/format` umbrella + dispatch table) — **doc family** (text·article·pdf·docx·pptx·xlsx) + `smartauto` via `format-select.cjs` |
| S5 | merged (#169) | Format Engine — **visual adapters** (mermaid·chart·dashboard·html·interactive·canvas); `smartauto` default flipped to `ALL_FORMATS`; `artifact-path.cjs` layout helper → **all 12 formats live** |
| **S6** | **this PR (final)** | 3 KPIs (`complete_verdict_rate`/`uncited_claim_rate`/`breaker_trip_rate`) + `SOP-AIOPS-005-deepask-runtime-contract` + `ai-ops-deepask` cost-bucket + **Phase-8 promotion → `operating`** (spec/retro → `wiki/capabilities/deepask/`) |

**deepask là `operating` từ 2026-05-30** (capability-registry `state: operating`). Full 5-stage loop + 12-format engine, breaker-safe + citation-disciplined, một zero-routing consumer của resolver-plan. Runtime contract: `SOP-AIOPS-005-deepask-runtime-contract`.

## Cờ

| Cờ | Giá trị | Hiệu ứng |
|---|---|---|
| `--format` | text·article·pdf·docx·pptx·xlsx·mermaid·chart·dashboard·html·interactive·canvas·**smartauto** (default) | format adapter (S4–S5; pre-S4 → `article`/`answer.md` only) |
| `--sources` | csv của \{pillars,ops,metrics,wiki,brain,external,scratch} · **auto** (default) | hạn chế loại IA nào deepask có thể chạm vào (truyền cho resolver-plan như một bộ lọc) |
| `--depth` | quick·**standard** (default)·deep·exhaustive | sub-need bound (≤6 standard / ≤12 deep) + parallel fan-out + adversarial-verify votes (0/1/2/3) |
| `--dry-run` | flag | hiển thị decomposition + **real** ResolverPlans + predicted coverage + cost estimate; thực hiện zero reads/runs/writes TRỪ một `ops.deepask_runs` dry-run row. NB: dry-run DOES call resolver-plan → consumes resolver-breaker budget → labeled honestly. |

## Phân phối (vòng lặp 5 giai đoạn)

0. **Pre-flight (orchestrator):** **resolver-budget accountant** — đọc `session_finds_count` mới nhất, gọi `scripts/deepask/breaker-budget.cjs` `computeBreakerBudget({sessionFindsCount, subNeedCount, ...})`; nếu không viable → emit honest PARTIAL up front (`gap_reason='breaker_budget'`), không bao giờ một `no_coverage` được tạo ra; nếu `capped_to_budget` → giảm depth và thông báo cho operator.
1. `deepask/decompose` — question → MECE sub-needs (≤6/≤12), IA-type A/B/C/D tags.
2. `deepask/orchestrator` → per sub-need call `resolver-plan` (`/resolver plan` / `mcp__supabase-ops__resolver_find`) → `ResolverPlan v1`.
3. `deepask/execute` — parallel subagents: **READ** `content_axis` (S1); **RUN** Tier-A `capability_axis` + surface Tier-B+ + delegate web→`deep-research` (S3). Tác giả concrete read-only SQL / `wiki_ask` question / skill params, **grounded trong `grounding_ref`/`columns_hint`; không bao giờ invents column names**.
4. `deepask/synthesize` (S2) — Pyramid + citation + authority + conflict + freshness + adversarial-verify → format-agnostic IR.
5. `deepask/completeness-critic` (S2) — coverage matrix + MECE + live-probe → COMPLETE | PARTIAL+remedy; ≤1 bounded follow-up.
6. Format (S4–S5) → artifact trong `.archives/deepask/<YYYY-MM-DD>-<slug>/` (luôn `answer.md` + `plan.json` + `sources.json` + rendered artifact).
7. **Observe** → `ops.deepask_runs` + `ops.deepask_coverage`; founder edit/reject → `ops.corrections`.

## Bảo vệ (luôn bật)
- **Firewall:** product data chỉ via `metrics.*` (hook `pre-tool-supabase-product`); không bao giờ `product.*`.
- **gbrain:** reads dưới $100/mo cap; ưu tiên `search`/`recall` hơn `think`.
- **resolver breaker:** 20-find/4h/session — budgeted bởi accountant (Stage 0).
- **Billing:** subscription trong session; API key out-of-band/embeddings only.
- **HITL:** runtime = Tier A (read/synthesize); bất kỳ Tier-B+ capability leg nào là **surfaced**, không bao giờ auto-run.

## Ranh giới
`/deepask` federates + **composes** `/brain` (gbrain) + `/wiki ask` + supabase-query + `/think` frameworks, delegating the web leg to `deep-research`. Nó KHÔNG route (đó là `resolver-plan`) và KHÔNG create/modify (đó là `/cla` · `/evolve` · `/update`). Internal-first.
