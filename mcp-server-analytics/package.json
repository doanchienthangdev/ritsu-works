{
  "name": "@ritsu/mcp-server-analytics",
  "version": "0.1.0",
  "description": "supabase-analytics MCP — read-only consumer MCP over the pseudonymized ritsu-analytics Door-2 dataset (live.*), connecting as the least-priv analytics_reader Postgres role. Capability: product-db-readonly-access, Sprint 1.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "smoke": "tsx src/cli/smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
